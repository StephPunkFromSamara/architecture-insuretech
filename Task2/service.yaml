apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
spec:
  selector:
    app: scaletestapp
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080        # порт внутри кластера
      targetPort: 8080  # порт контейнера
      nodePort: 30080 # порт на хосте minikube